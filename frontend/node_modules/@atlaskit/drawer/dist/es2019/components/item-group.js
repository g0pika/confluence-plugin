/** @jsx jsx */
import { jsx } from '@emotion/core';
import { ItemGroup } from '@atlaskit/item';
import { 
// @ts-ignore - it's exported the type is just missing
fontSizeSmall, gridSize as gridSizeFn, } from '@atlaskit/theme/constants';
const groupTitleFontSize = fontSizeSmall();
const gridSize = gridSizeFn();
const SkeletonGroupTitle = ({ ...props }) => (jsx("div", Object.assign({ css: {
        fontSize: `${groupTitleFontSize}px`,
        lineHeight: `${(gridSize * 2) / groupTitleFontSize}`,
        fontWeight: 600,
        marginTop: `${gridSize * 1.5}px`,
    } }, props)));
const ItemChildrenWrapper = ({ ...props }) => (jsx("div", Object.assign({ css: { marginLeft: `${gridSize}px` } }, props)));
const ItemGroupWrapper = ({ ...props }) => (jsx("div", Object.assign({ css: { paddingRight: `${gridSize * 4}px` } }, props)));
export const DrawerItemGroup = ({ title, isCompact, children }) => {
    const wrappedTitle = title ? (jsx(SkeletonGroupTitle, null, title)) : null;
    return (jsx(ItemGroupWrapper, null,
        jsx(ItemGroup, { title: wrappedTitle, isCompact: isCompact },
            jsx(ItemChildrenWrapper, null, children))));
};
//# sourceMappingURL=item-group.js.map