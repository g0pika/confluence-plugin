import React, { Component } from 'react';
import ToggleStateless from './ToggleStateless';
// This component is a thin wrapper around the stateless component that manages
// the isChecked state for you
export default class Toggle extends Component {
    constructor() {
        super(...arguments);
        this.state = {
            isChecked: this.props.isDefaultChecked,
        };
        this.onChange = (event) => {
            this.setState({ isChecked: !this.state.isChecked });
            this.props.onChange(event);
        };
    }
    render() {
        return (React.createElement(ToggleStateless, Object.assign({}, this.props, { isChecked: this.state.isChecked, onChange: this.onChange })));
    }
}
Toggle.defaultProps = {
    isDisabled: false,
    onBlur: () => { },
    onChange: () => { },
    onFocus: () => { },
    size: 'regular',
    label: '',
    name: '',
    value: '',
    isDefaultChecked: false,
};
//# sourceMappingURL=Toggle.js.map