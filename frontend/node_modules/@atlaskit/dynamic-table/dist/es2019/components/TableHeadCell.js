import React from 'react';
import { HeadCell } from '../styled/TableHead';
class TableHeadCell extends React.Component {
    render() {
        const { content, inlineStyles, testId, ...rest } = this.props;
        return (React.createElement(HeadCell, Object.assign({ style: inlineStyles, "data-testid": testId && `${testId}--head--cell` }, rest, { tabIndex: rest.isSortable ? 0 : undefined }),
            React.createElement("span", null, content)));
    }
}
TableHeadCell.defaultProps = {
    innerRef: () => { },
    inlineStyles: {},
};
export default TableHeadCell;
//# sourceMappingURL=TableHeadCell.js.map