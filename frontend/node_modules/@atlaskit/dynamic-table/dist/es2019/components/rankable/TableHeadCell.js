import React from 'react';
import withDimensions from '../../hoc/withDimensions';
import { inlineStylesIfRanking } from '../../internal/helpers';
import HeadCell from '../TableHeadCell';
class RankableTableHeadCell extends React.Component {
    render() {
        const { isRanking, refHeight, refWidth, innerRef, ...restProps } = this.props;
        const inlineStyles = inlineStylesIfRanking(isRanking, refWidth);
        return (React.createElement(HeadCell, Object.assign({ inlineStyles: inlineStyles, innerRef: innerRef }, restProps)));
    }
}
export default withDimensions(RankableTableHeadCell);
//# sourceMappingURL=TableHeadCell.js.map