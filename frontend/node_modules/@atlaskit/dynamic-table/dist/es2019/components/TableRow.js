import React from 'react';
import { TableBodyCell } from '../styled/TableCell';
import { TableBodyRow } from '../styled/TableRow';
const Row = ({ row, head, testId, isFixedSize, isHighlighted }) => {
    const { cells, ...restRowProps } = row;
    return (React.createElement(TableBodyRow, Object.assign({}, restRowProps, { isHighlighted: isHighlighted, "data-testid": testId && `${testId}--row-${restRowProps.key}` }), cells.map((cell, cellIndex) => {
        const { content, ...restCellProps } = cell;
        const { shouldTruncate, width } = (head || { cells: [] }).cells[cellIndex] || {};
        return (React.createElement(TableBodyCell, Object.assign({ "data-testid": testId && `${testId}--cell-${cellIndex}` }, restCellProps, { isFixedSize: isFixedSize, key: cellIndex, shouldTruncate: shouldTruncate, width: width }), content));
    })));
};
export default Row;
//# sourceMappingURL=TableRow.js.map