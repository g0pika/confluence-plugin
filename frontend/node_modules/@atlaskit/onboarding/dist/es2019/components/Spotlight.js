import React from 'react';
import SpotlightInner from './SpotlightInner';
import { SpotlightConsumer } from './SpotlightManager';
class Spotlight extends React.Component {
    render() {
        const { targetNode, target, testId = 'spotlight', ...rest } = this.props;
        return (React.createElement(SpotlightConsumer, null, ({ opened, closed, targets }) => {
            // use the targetNode prop or try get the target from context targets using name
            const actualTargetNode = targetNode ||
                (typeof target === 'string' ? targets[target] : undefined);
            return actualTargetNode ? (React.createElement(SpotlightInner, Object.assign({}, rest, { targetNode: actualTargetNode, target: target, onOpened: opened, onClosed: closed, testId: testId }))) : null;
        }));
    }
}
Spotlight.defaultProps = {
    dialogWidth: 400,
    pulse: true,
};
export default Spotlight;
//# sourceMappingURL=Spotlight.js.map