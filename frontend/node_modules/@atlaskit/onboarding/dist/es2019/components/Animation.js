import React from 'react';
import { Transition } from 'react-transition-group';
const duration = {
    enter: 0,
    exit: 100,
};
export const Fade = ({ in: hasEntered, children, onExited }) => (React.createElement(Transition, { in: hasEntered, timeout: duration, onExited: onExited, unmountOnExit: true, appear: true }, (status) => {
    const base = {
        transition: `opacity ${duration.exit}ms`,
        opacity: 0,
    };
    const anim = {
        entered: { opacity: 1 },
        exiting: { opacity: 0 },
    };
    const style = { ...base, ...anim[status] };
    return children(style);
}));
//# sourceMappingURL=Animation.js.map