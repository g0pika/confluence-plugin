import React from 'react';
import styled from 'styled-components';
import { gridSize } from '@atlaskit/theme/constants';
import { Label } from './styled/Field';
const FieldsetLabel = styled(Label) `
  margin-bottom: 0;
`;
const Fieldset = styled.fieldset `
  margin-top: ${gridSize}px;
`;
export default ({ children, legend }) => (React.createElement(Fieldset, null,
    legend && (React.createElement("legend", null,
        React.createElement(FieldsetLabel, null, legend))),
    children));
//# sourceMappingURL=Fieldset.js.map