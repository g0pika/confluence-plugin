import React, { Component } from 'react';
import FormSectionWrapper, { FormSectionDescription, FormSectionTitle, } from './styled/FormSection';
export default class FormSection extends Component {
    render() {
        const { title, description, children } = this.props;
        return (React.createElement(FormSectionWrapper, null,
            title && React.createElement(FormSectionTitle, null, title),
            description && (React.createElement(FormSectionDescription, null, description)),
            children));
    }
}
//# sourceMappingURL=FormSection.js.map