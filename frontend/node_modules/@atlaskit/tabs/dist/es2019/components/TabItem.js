import React, { Component } from 'react';
import { NavItem, NavLine } from '../styled';
const noop = () => { };
export default class TabItem extends Component {
    render() {
        const { data, elementProps, innerRef, isSelected } = this.props;
        return (React.createElement(NavItem, Object.assign({}, elementProps, { innerRef: innerRef, status: isSelected ? 'selected' : 'normal' }),
            data.label,
            isSelected && React.createElement(NavLine, { status: "selected" })));
    }
}
TabItem.defaultProps = {
    data: {},
    elementProps: {},
    innerRef: noop,
    isSelected: false,
};
//# sourceMappingURL=TabItem.js.map